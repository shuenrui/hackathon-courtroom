server {
    listen 80;
    server_name _;
    root /app;
    index index.html;

    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;
    gzip_min_length 1024;

    # always serve fresh HTML and queue data; audio caches fine (immutable names)
    location = /index.html {
        add_header Cache-Control "no-cache";
    }

    location /segments/ {
        add_header Cache-Control "no-cache";
        try_files $uri =404;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
